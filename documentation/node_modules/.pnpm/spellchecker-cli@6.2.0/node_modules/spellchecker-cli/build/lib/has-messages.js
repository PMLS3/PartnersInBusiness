import constant from 'lodash/constant.js';
import sumBy from 'lodash/sumBy.js';
export var hasMessages = function (vfiles, filterMessages) {
    if (filterMessages === void 0) { filterMessages = constant(true); }
    var messageCount = sumBy(vfiles, function (file) { return file.messages.filter(filterMessages).length; });
    return messageCount > 0;
};
