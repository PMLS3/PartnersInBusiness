/**
 * Plugin to classify @mentions as source, which represents “external
 * (ungrammatical) values” instead of natural language.
 * This hides mentions from `retext-spell`, `retext-readability`,
 * `retext-equality`.
 *
 * @type {import('unified').Plugin<[Options?], Root>}
 */
export default function retextSyntaxMentions(
  options?: Options | undefined
):
  | void
  | import('unified').Transformer<import('nlcst').Root, import('nlcst').Root>
export type Root = import('nlcst').Root
export type Sentence = import('nlcst').Sentence
export type Word = import('nlcst').Word
export type Source = import('nlcst').Source
export type SentenceContent = import('nlcst').SentenceContent
export type WordContent = import('nlcst').WordContent
/**
 * Configuration.
 */
export type Options = {
  /**
   * Style can be either `'github'` (for GitHub user and team mentions),
   * `'twitter'` (for Twitter handles), or a regular expression (such as
   * `/^@\w{1,15}$/i`, which is the Twitter regex).
   */
  style?: 'github' | 'twitter' | RegExp | null | undefined
}
